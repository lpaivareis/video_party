<%= form_with(model: party) do |form| %>
  <% if party.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(party.errors.count, "error") %> prohibited this party from being saved:</h2>

      <ul>
        <% party.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-floating mb-3">
    <%= form.text_field :title, class: "form-control", id: "floatingInput", placeholder: "Nome da sala" %>
    <%= form.label "Nome da sala", style: "display: block", for:"floatingInput" %>
  </div>

  <div class="form-floating mb-3">
    <%= form.text_field :embeded_link, class: "form-control", id: "floatingInput", placeholder: "Aqui vai o link do video" %>
    <%= form.label "Link do video", style: "display: block", for: "floatingInput" %>
  </div>

  <div class="text-end">
    <%= form.submit class: "btn btn-success" %>
  </div>
<% end %>
